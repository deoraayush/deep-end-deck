export function BetaBadge() {
  return (
    <span className="inline-flex items-center rounded-full border-2 border-[color:var(--orange)] px-3 py-1 font-display text-[10px] font-extrabold uppercase tracking-widest text-[color:var(--orange)]">
      Beta — free during testing
    </span>
  );
}
